.grid-container
  %section.usa-section
    = form_for resource, as: resource_name, url: session_path(resource_name), data: {turbo: false}, html: {class: 'usa-form usa-form--large margin-x-auto'} do |f|
      %h1 Sign in
      %fieldset.usa-fieldset
        = render "devise/shared/error_messages", resource: resource

        = f.label :email, class: 'usa-label' do
          Email address
        = f.email_field :email, required: true, autofocus: true, autocomplete: :email, class: 'usa-input'

        = f.label :password, class: 'usa-label' do
          Password
        = f.password_field :password, required: true, autocomplete: "current-password", class: 'usa-input'

        = f.submit "Sign in", class: 'usa-button width-full'

        -#%p
          = link_to new_password_path(resource_name) do
            Forgot your password?
